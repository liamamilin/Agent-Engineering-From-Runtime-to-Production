# Start Here — OpenCode

Use `OPENCODE_MASTER_PROMPT.md` as the governing execution prompt.

Do not start writing chapters until you have read `docs/ONTOLOGY.md` and `docs/ARCHITECTURE.md`.

First command-level objective: complete **Phase 0** in `OPENCODE_TASKS.md` and prove the minimal runtime with offline tests. Then continue module-by-module.
