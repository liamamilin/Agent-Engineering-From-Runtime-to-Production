# Quiz — M2

1. Tool schema 的主要用途是什么？
   - A) 装饰工具函数
   - B) 定义工具的参数、类型和权限要求
   - C) 替代 API 文档
   - D) 加密工具参数

2. 以下哪种工具需要权限控制？
   - A) search（搜索信息）
   - B) calculator（计算表达式）
   - C) write_file（写入文件）
   - D) get_time（获取当前时间）

3. 当工具参数验证失败时，应该怎么做？
   - A) 使用默认值继续执行
   - B) 返回 ToolResult.fail，不执行工具
   - C) 抛出异常，终止整个 Agent
   - D) 忽略错误，尝试执行

4. 工具执行结果应该如何处理？
   - A) 直接打印给用户
   - B) 转换为 Observation，进入下一轮决策
   - C) 存储在数据库中
   - D) 丢弃，只保留最终答案

5. 幂等工具的特征是什么？
   - A) 执行速度快
   - B) 多次执行产生相同结果
   - C) 不需要参数
   - D) 不访问外部资源
