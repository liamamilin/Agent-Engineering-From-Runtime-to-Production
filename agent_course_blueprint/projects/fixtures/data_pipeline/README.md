# Data Pipeline

A data processing pipeline for CSV files.

## Setup

```bash
pip install -r requirements.txt
```

## Usage

```bash
python pipeline/main.py --input data/input.csv --output data/output.csv
```

## Pipeline Stages

1. Load data from CSV
2. Clean and validate data
3. Transform data
4. Export results
