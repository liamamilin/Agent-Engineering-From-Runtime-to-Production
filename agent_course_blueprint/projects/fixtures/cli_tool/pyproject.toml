[build-system]
requires = ["setuptools>=45"]
build-backend = "setuptools.build_meta"

[project]
name = "file-tool"
version = "0.1.0"
dependencies = ["click>=8.0"]

[project.scripts]
file-tool = "cli.main:cli"
